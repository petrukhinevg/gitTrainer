# Project Board

Local description of the project board for `gitTrainer`.

Repository: https://github.com/petrukhinevg/gitTrainer

Online board: https://github.com/users/petrukhinevg/projects/4

Project custom field: `Pairs with` stores the counterpart backend/frontend issue when a child task has a paired issue on the other side.

## Columns
- `Backlog`: everything captured but not ready for implementation
- `Ready`: tasks with a clear result and acceptance criteria
- `In Progress`: active implementation
- `Review`: implemented and waiting for review
- `Done`: accepted and completed

### Ready

- `#138` Sub-issue 1.1. Deliver catalog route shell and swappable provider seam
- `#149` Sub-issue 1.2. Deliver catalog query policy for filtering and sorting
- `#150` Sub-issue 1.2. Deliver catalog query controls and browse-state handling
- `#151` Sub-issue 1.3. Deliver MVP catalog summary content and browse-state fixtures
- `#152` Sub-issue 1.3. Deliver catalog list rendering and entry-action presentation

### In Progress

- `#98` Parent Issue 1. Scenario catalog browsing MVP

### Review

- `#97` Rework roadmap for independent vertical epics
- `#137` Sub-issue 1.1. Deliver catalog browse API shell and deterministic stub boundary

### Backlog

- `#99` Parent Issue 2. Exercise context and workspace MVP
- `#100` Parent Issue 3. Submission and correctness MVP
- `#101` Parent Issue 4. Guided retry and hints MVP
- `#102` Parent Issue 5. Progress and next-step guidance MVP
- `#139` Sub-issue 2.1. Deliver exercise detail API shell and stub workspace payload
- `#140` Sub-issue 2.1. Deliver exercise route, workspace shell, and swappable detail provider seam
- `#141` Sub-issue 3.1. Deliver session lifecycle and submission transport boundary
- `#142` Sub-issue 3.1. Deliver answer input shell and local draft-state flow
- `#143` Sub-issue 4.1. Deliver retry state model and retry eligibility policy
- `#144` Sub-issue 4.1. Deliver feedback panel shell and preserved exercise context state
- `#145` Sub-issue 5.1. Deliver attempt outcome storage model and completion event recording
- `#146` Sub-issue 5.1. Deliver progress surface shell and status marker components
- `#153` Sub-issue 2.2. Deliver task-content structure and ordered-step assembly rules
- `#154` Sub-issue 2.2. Deliver task instruction and ordered-step presentation
- `#155` Sub-issue 2.3. Deliver repository context schema and MVP scenario detail content
- `#156` Sub-issue 2.3. Deliver repository context presentation surfaces
- `#157` Sub-issue 3.2. Deliver first answer-type validation rules and outcome model
- `#158` Sub-issue 3.2. Deliver submission transport integration and request-state handling
- `#159` Sub-issue 3.3. Deliver submission failure policy and unsupported-answer mappings
- `#160` Sub-issue 3.3. Deliver correctness and unsupported-answer feedback rendering
- `#161` Sub-issue 4.2. Deliver explanation selection and progressive hint policy
- `#162` Sub-issue 4.2. Deliver explanation rendering and progressive hint interactions
- `#163` Sub-issue 4.3. Deliver retry feedback payload assembly and boundary mapping
- `#164` Sub-issue 4.3. Deliver retry boundary integration and retry-state transitions
- `#165` Sub-issue 5.2. Deliver progress summary contract and status derivation rules
- `#166` Sub-issue 5.2. Deliver progress summary integration and activity-state handling
- `#167` Sub-issue 5.3. Deliver next-step recommendation policy and payload mapping
- `#168` Sub-issue 5.3. Deliver recommendation presentation and follow-up guidance UX

## Update rule

- New work should first be recorded in the roadmap or issue list.
- When a task is taken into work, move it to `In Progress`.
- After implementation, move it to `Review`.
- After acceptance, move it to `Done`.

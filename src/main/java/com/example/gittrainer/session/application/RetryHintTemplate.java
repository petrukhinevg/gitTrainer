package com.example.gittrainer.session.application;

public record RetryHintTemplate(
        String nudgeTitle,
        String nudgeMessage,
        String strongTitle,
        String strongMessage
) {
}

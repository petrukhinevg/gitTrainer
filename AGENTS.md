# Инструкции для агента репозитория

Приоритет языка: по умолчанию всегда используй русский язык в ответах, задачах, комментариях, PR и документации репозитория.
Исключения: не переводи код, команды, API-запросы, имена полей, ключевые слова платформ, commit message format, служебные идентификаторы и устойчивые технические выражения там, где перевод ломает смысл, синтаксис или ожидаемое поведение.

Этот файл содержит и быстрый bootstrap для новых сессий, и общие правила работы в репозитории.

## Старт нового чата

0. Всегда отвечай и работай только на русском языке
1. Прочитай этот файл целиком.
2. Сначала открой `docs/ROADMAP.md` и `docs/ARCHITECTURE.md`.
3. Затем открывай только те дополнительные документы, которые действительно нужны для текущей задачи, по маршрутизации ниже.

## Кратко о продукте

- Продукт: одностраничное веб-приложение для изучения и практики Git через интерактивные сценарии, миссии и обратную связь по прогрессу
- Цикл MVP: выбрать тренировочный сценарий -> изучить задачу и состояние репозитория -> ввести или собрать Git-действия -> получить проверку и объяснение
- Backend отвечает за: определения сценариев, состояние упражнений, проверку ответов, отслеживание прогресса, оценивание, подсказки и правила генерации объяснений
- Frontend отвечает за: маршрутизацию SPA, интерфейс тренировочного рабочего пространства, клиентское состояние взаимодействия, отображение прогресса и рендеринг обратной связи

Проект ориентирован на пользователей, которые понимают базовые процессы разработки, но хотят безопасно и практически отрабатывать Git. Приложение должно помогать понять не только какую команду выполнить, но и почему именно она подходит для текущего состояния репозитория и цели задания.

## Кратко о репозитории

- Корневой проект: единый Gradle-проект со Spring Boot 4 и Java 21
- `frontend/`: SPA-приложение для тренировочного интерфейса
- `docs/`: roadmap, архитектура, workflow и локальные заметки по доске
- Другие важные каталоги: `src/main/java`, `src/test/java`, `src/main/resources`

## Маршрутизация сессии

Читай эти документы в каждом новом чате:

1. `docs/ROADMAP.md` для понимания границ продукта, рамок MVP и списка задач.
2. `docs/ARCHITECTURE.md` для понимания границ backend/frontend и размещения пакетов.

Открывай эти документы только если они нужны для текущей задачи:

3. `docs/TRACKER_WORKFLOW.md` для статусов задач, декомпозиции, потока веток и PR, статуса review или обновления board.
4. `docs/BACKEND_ROADMAP.md` для backend-only задач или изменений API, данных и моделей.
5. `docs/FRONTEND_ROADMAP.md` для frontend-only задач или изменений UX/UI.
6. `.env` только если задаче действительно нужны локальные креды или токены.

## Маршрутизация по типу действия

- Если задача меняет рамки продукта, границы MVP или требует создания либо уточнения work items, смотри `docs/ROADMAP.md`.
- Если задача меняет зоны ответственности backend/frontend или размещение пакетов, смотри `docs/ARCHITECTURE.md`.
- Если задача связана с ветками git, коммитами, push, созданием PR, исправлениями по ревью или проверками перед push, смотри этот файл и `docs/TRACKER_WORKFLOW.md`.
- Если задача связана со статусами issue, декомпозицией, структурой parent/sub-issue, статусом review или обновлением project board, смотри `docs/TRACKER_WORKFLOW.md`.
- Если задача только про backend, смотри `docs/BACKEND_ROADMAP.md`.
- Если задача только про frontend, смотри `docs/FRONTEND_ROADMAP.md`.
- Если задаче нужны локальные креды, токены или аутентифицированные локальные инструменты, смотри `.env`.

## Локальные файлы и доступы

- Не коммить `.env`.
- Используй `GIT_USERNAME` и `GIT_TOKEN` для git-действий и операций с удалённой платформой, если нужны аутентифицированные действия.
- Админские доступы используй только для owner-level операций над репозиторием.
- Не полагайся на парольную аутентификацию, если этого явно не требуют.
- Проверяй `.env` только тогда, когда задаче действительно нужны локальные доступы или токены.

## Подсказки по процессу

- Используй `docs/TRACKER_WORKFLOW.md` как единый источник истины для декомпозиции задач, потока веток, PR-flow, состояний доски, работы с WIP дочерних задач и момента merge.
- Переиспользуй примеры команд из `docs/TRACKER_WORKFLOW.md` для `gh`, linked branches, привязки issue/PR через GraphQL и epic-based PR flow вместо того, чтобы каждый раз собирать эти API-вызовы заново.
- Если в ходе задачи обнаружились стабильные `gh`-команды, API-запросы, GraphQL mutation или другие повторяемые операционные заметки, которые позже снова потребуют поиска по документации, добавь их в соответствующие инструкции репозитория до завершения работы.
- При создании issue всегда назначай корректный набор labels по фактическому scope и стороне задачи, а не оставляй labels пустыми и не используй заглушки.
- По умолчанию работай только в текущем локальном каталоге репозитория.
- Не создавай и не переключайся на альтернативные локальные папки проекта, соседние клоны репозитория или `git worktree`, если пользователь явно не попросил такой режим.
- Если на диске уже есть другие локальные копии, игнорируй их и продолжай работу в текущем каталоге репозитория, если пользователь явно не указал иное.
- Не делай push в `origin` во время активной реализации, если пользователь явно этого не попросил.
- Когда задача переводится в `Review`, сначала запушь ветку задачи и создай или обнови её PR.
- Если ветка задачи нацелена в `main`, проверь, что PR виден в поле проекта `Linked pull requests`, прежде чем считать передачу в review завершённой.
- Если ветка задачи нацелена не в `main`, например в epic branch, не блокируй перевод в `Review` ожиданием заполнения `Linked pull requests`.
- В теле PR используй `Closes #<issue>`, `Fixes #<issue>` или `Resolves #<issue>` только если PR нацелен в `main` и должен закрыть issue после merge.
- Для PR в epic branch или для случаев, где нужна только связь без автозакрытия, используй `Refs #<issue>`; простой `#<issue>` или `Refs #<issue>` создаёт mention/reference, а не closing linkage.
- Используй формат коммита из `docs/TRACKER_WORKFLOW.md`: `number_ShortCommitDescription` для основного implementation commit задачи.
- После создания epic branch сделай начальный epic commit в этой ветке до создания любых дочерних task branches.
- После появления начального epic commit сразу создай дочерние task branches для текущих sub-issues этого эпика от обновлённой головы epic branch.
- При работе внутри эпика всегда ответвляй каждую дочернюю задачу непосредственно от epic branch, а не от другой child task branch.
- Не продвигай локально `main` или epic branch только ради продолжения разработки следующих дочерних задач.
- Merge дочерней task branch в epic branch допустим, когда без этой интеграции дальнейшая разработка действительно заблокирована.
- Исправления по review оставляй в той же task branch. Не создавай отдельную review-fix branch.
- При необходимости добавляй один дополнительный commit с суффиксом `review fix`.
- Текущее поле проекта для видимости PR — `Linked pull requests`. Не создавай кастомную замену этому полю, если настройка проекта не изменилась.
- При создании дочерних task branches также создавай или регистрируй соответствующую linked branch для issue, чтобы GitHub мог автоматически заполнить `Linked pull requests`.
- Для веток задач, которые нацелены в `main`, не оставляй задачу в состоянии, где PR уже существует, а на доске всё ещё висит placeholder-ссылка; исправляй привязку в рамках того же handoff.

## Проверки перед отправкой

- Перед каждым `git push` запускай `./gradlew check`.
- Если изменялся backend-код, по возможности запускай `./gradlew test`.
- Если в будущем появится каталог `frontend/` и изменялся frontend-код, перед push запускай production build этой части внутри `frontend/`.

## Границы изменений и гигиена правок

- Если локальные изменения уже разрослись или работа переключается на другую задачу, сначала закоммить текущую логическую единицу.
- Держи коммиты сфокусированными: не дроби одну задачу на шумные микрокоммиты, но и не смешивай в одном коммите несвязанные изменения.
- Для крупных изменений предпочитай чёткие границы пакетов и простую расширяемую структуру.
- Держи бизнес-логику для тренировочных Git-сценариев, валидации, подсказок и отслеживания прогресса внутри capability-пакетов, названных по реальной предметной области, а не в общих shared bucket.
- Используй `docs/ARCHITECTURE.md` для границ backend/frontend.
- Используй roadmap-документы как источник новых issue.
- Если запрошенное изменение там ещё не описано, сначала обнови roadmap или заметки по board, а уже потом начинай крупную реализацию.

## Заметки

- Этот файл является единственной точкой входа для новых сессий и правил работы по репозиторию.
- Локальные секреты храни в `.env`.
